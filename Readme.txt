-- Generate project
npm init

-- Create Git
$> git init

use file .gitignore to exclude files

-- Snapshot for the first time
$>git add .

-- confirm changes
$> git commit -m "Estado Inicial"

-- go to github
-- Create new repository
-- 
$> git remote add <URL>
$> git push -u origin master


/*******************************************/
WSS

$> npm install ws --save
$> npm install fs --save
$> npm install https --save

$> npm i firebase